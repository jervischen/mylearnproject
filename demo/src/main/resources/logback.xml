<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoder 默认配置为PatternLayoutEncoder -->
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- logback为java中的包 -->
  <!--  <logger name="logback"/>-->


    <appender name="serverAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>C:\Users\Administrator\Desktop\mygit\mylearnproject\mylearnproject\demo\src\main\server.log</file>
        <append>true</append>
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
      <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/server.%d{yyyy-MM-dd_HH}.log</fileNamePattern>
        </rollingPolicy>
    </appender>

    <appender name="asyncServerAppender" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="serverAppender"/>
        <queueSize>10000</queueSize>
    </appender>

<!--    <logger name="logback" level="ERROR" additivity="true">
        <appender-ref ref="serverAppender"/>
    </logger>-->
    <root level="info">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="asyncServerAppender"/>
      <!--  <appender-ref ref="serverAppender" />-->
    </root>

</configuration>